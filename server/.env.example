# Server
PORT=8080
NODE_ENV=development

# Database
DATABASE_URL="file:./dev.db"

# CORS — URL du frontend autorisé (obligatoire en production)
ALLOWED_ORIGIN=http://localhost:4200

# Logging
LOG_LEVEL=info

# Logstash (optionnel — désactivé si absent)
LOGSTASH_HOST=logstash
LOGSTASH_PORT=5000
